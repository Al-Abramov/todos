import { filteredTodo } from '../../../../store/todo-slice';

console.log();
/*jest.mock('../../../../store/todo-slice', {
  filteredTodo: jest.fn(),
});

describe('name', () => {
  it('', () => {
    const wrapper = render(<></>);

    wrapper.element.click();

    expect(filteredTodo).toBeCalledTimes(1);
    expect(filteredTodo).toBeCalledWith('id');
  })
})*/
